import { parseInput,arrSum,hasDuplicates} from "./helpers.js";
//split into a 2d string array
let input = (await parseInput("y.txt","\n"))
.map(el=>el.split(" ")
.map(el=>{
  if(el){

  }
}))


let directory:any[] = []

class Node{
  children: Node[]
  values:number[]
  constructor(){
    this.children = []
    this.values = []
  }

  addChild(child:Node|number){

  }
}

